{% import 'ui/Macro/money.html.twig' as money %}

<section class="container mb-5 commande-success">
    <div class="row d-flex justify-content-center">
        <div class="col-12 col-md-8 col-lg-7">
            <div class="card">
                <div class="card-body p-3">
                    <div class="row">
                        <div class="col-12">
                            <h1 class="h1-responsive font-weight-bold text-dark">Félicitation <span>🎉</span></h1>
                            <hr>
                        </div>
                        <div class="col-12">
                            <p class="alert alert-primary small-9 font-weight-stone-500 mb-4 p-2">
                                Un message vous sera envoyer par mail, une fois votre reservation confirmer.
                                Ce message contiendra votre <strong class="font-weight-stone-600 text-dark">numéro de reservation</strong>.
                            </p>
                        </div>
                        <div class="col-12">
                            <h3 class="h3-responsive font-weight-stone-600 text-dark mb-4">Votre réservation</h3>
                            <div class="booking-room-detail mb-4">
                                {{ include('site/commande/success/_room_info.html.twig') }}
                            </div>

                            <div class="booking-detail">
                                <h3 class="h3-responsive font-weight-stone-600 text-dark mb-4">Détail de la réservation</h3>
                                <div class="d-flex justify-content-between">
                                    <div class="title">Date d'arrivée</div>
                                    <div class="content text-dark">{{ date(booking.checkin)|format_datetime(pattern="EEEE dd MMM yyyy")|title }}</div>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <div class="title">Date de départ</div>
                                    <div class="content text-dark">{{ date(booking.checkout)|format_datetime(pattern="EEEE dd MMM yyyy")|title }}</div>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <div class="title">Durée totale du séjour</div>
                                    <div class="content text-dark">{{ booking.days > 1 ? booking.days ~ ' nuits' : booking.days ~ ' nuit' }}</div>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <div class="title">Occupants</div>
                                    <div class="content text-dark">
                                        {% if booking.children %}
                                            {{ booking.adult }} adultes et {{ booking.children }} enfant par chambre
                                        {% else %}
                                            {{ booking.adult }} adultes par chambre
                                        {% endif %}
                                    </div>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <div class="title">Nombre de chambre</div>
                                    <div class="content text-dark">{{ booking.roomNumber }}</div>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <div class="title">Montant total</div>
                                    <div class="content text-dark">{{ money.convertAndFormat(commande.amountPaid) }}</div>
                                </div>
                            </div>
                            <div class="d-flex {% if app.user %}justify-content-between{% else %}justify-content-center{% endif %} mt-4">
                                <a href="{{ path('app_home') }}" class="btn btn-primary btn-block z-depth-0 {% if app.user %}mr-1{% endif %}">
                                    <i class="fas fa-home mr-2"></i> Retour à l'accueil
                                </a>

                                {% if app.user %}
                                    <a href="{{ path('app_user_booking_index') }}" class="btn btn-default btn-block z-depth-0 ml-1">
                                        Mes reservations <i class="fas fa-long-arrow-alt-right ml-2"></i>
                                    </a>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

