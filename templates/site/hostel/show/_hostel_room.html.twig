<div class="row">
    <div class="col-12">
        <h3 class="h3-responsive font-weight-bold text-dark mb-4">Choisissez votre chambre</h3>
    </div>
    <div class="col-12">
        {% if hostel.rooms|length > 0 %}
            <div class="row">
                {% for room in hostel.rooms %}
                    <div class="col-12 col-md-6 col-lg-4">
                        {{ include('site/hostel/show/_box.html.twig') }}
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
</div>
