{% extends layout %}

{% import "mails/macros.twig" as e %}

{% block body %}

    {% apply markdown_email %}

        Bonjour {{ partner.username ? partner.username : partner.firstnane }}
        ===============================

        {% if format == 'html' %}
            {{ e.section(data.content|markdown_untrusted) }}
        {% endif %}
    {% endapply %}

    {{ e.button('Tableau de bord', url('app_partner_index')) }}
{% endblock %}
