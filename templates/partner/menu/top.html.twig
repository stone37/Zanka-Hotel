<nav class="navbar fixed-top navbar-expand-lg double-nav py-0" style="background-color: #ffffff !important">
    <div class="float-left" >
        <a href="#" data-activates="slide-out"
           class="button-collapse"><i class="fas fa-bars"></i>
        </a>
    </div>

    <div class="d-flex ml-auto change-mode">
        <ul class="nav navbar-nav nav-flex-icons">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                {{ render(controller('App\\Controller\\NotificationController::index')) }}

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle avatar waves-effect text-secondary" href="#"
                       id="userDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <span class="user-prefix d-inline-block rounded-circle">{{ userPrefixName(app.user) }}</span>
                    </a>
                    <div class="dropdown-menu dropdown-dark dropdown-menu-right" aria-labelledby="userDropdown">
                        <a class="dropdown-item" href="{{ path('app_partner_profil_edit') }}">
                            <i class="fas fa-user-edit mr-2" aria-hidden="true"></i>
                            Modifier le profil
                        </a>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">
                            <i class="fas fa-power-off mr-2" aria-hidden="true"></i>
                            Déconnexion
                        </a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

